intent: strimzi-operator
flavor: helm
version: '1.0'
clouds:
- aws
- azure
- gcp
- kubernetes
description: Strimzi Kafka Operator deployed via Helm chart for managing Kafka clusters
  on Kubernetes
intentDetails:
  type: Operators
  description: Strimzi Kafka Operator for managing Apache Kafka clusters on Kubernetes
  displayName: Strimzi Operator
  iconUrl: https://raw.githubusercontent.com/Facets-cloud/facets-modules-redesign/main/icons/strimzi-operator.svg
spec:
  title: Strimzi Kafka Operator
  description: Installs and manages the Strimzi Operator using Helm for Apache Kafka
    deployment
  type: object
  properties:
    namespace:
      type: string
      title: Namespace
      description: Kubernetes namespace for Strimzi Operator installation
      default: kafka-system
      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
      maxLength: 63
    resources:
      type: object
      title: Resource Allocation
      description: CPU and memory resources for Strimzi Operator pods
      properties:
        cpu_request:
          type: string
          title: CPU Request
          description: Minimum CPU allocation
          default: 200m
        cpu_limit:
          type: string
          title: CPU Limit
          description: Maximum CPU allocation
          default: '1'
        memory_request:
          type: string
          title: Memory Request
          description: Minimum memory allocation
          default: 256Mi
        memory_limit:
          type: string
          title: Memory Limit
          description: Maximum memory allocation
          default: 1Gi
    helm_values:
      type: object
      title: Helm Values
      description: Additional Helm values to override defaults
      x-ui-yaml-editor: true
  required:
  - resources
inputs:
  kubernetes_cluster:
    type: '@facets/kubernetes-details'
    optional: false
    displayName: Kubernetes Cluster
    description: Target Kubernetes cluster for Strimzi Operator deployment
    providers:
    - kubernetes
    - helm
  node_pool:
    type: '@facets/kubernetes_nodepool'
    optional: false
    displayName: Node Pool
    description: Node pool for Strimzi Operator pods
outputs:
  default:
    type: '@facets/strimzi-operator'
    title: Strimzi Kafka Operator
iac:
  validated_files:
  - main.tf
  - variables.tf
  - locals.tf
  - outputs.tf
sample:
  kind: strimzi-operator
  flavor: helm
  version: '1.0'
  disabled: false
  spec:
    namespace: kafka
    resources:
      cpu_request: 200m
      cpu_limit: '1'
      memory_request: 256Mi
      memory_limit: 1Gi